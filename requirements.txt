# Highrise Bot Framework
highrise==0.0.35

# Core dependencies (usually already installed)
asyncio-contextmanager>=1.0.0
aiohttp>=3.8.0
requests>=2.28.0

# Optional enhancement packages
python-dotenv>=0.20.0
